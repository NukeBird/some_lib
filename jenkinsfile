pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('show files') { 
            steps { 
                sh 'ls' 
            }
        }
    }
}
